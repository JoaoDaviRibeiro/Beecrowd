#include <stdio.h>
 
int main() {
 
    int numUm;
    int numDois;
    
    scanf("%d", &numUm);
    scanf("%d", &numDois);
    
    int soma;
    
    soma = numUm + numDois;
    
    printf("X = %d", soma);
 
    return 0;
}
